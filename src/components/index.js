import React from "react"
import ReactDom, { createRoot } from "react-dom/client"
import N from "./components/dd"
const root = ReactDom.createRoot(document.getElementById("root"))

root.render(
   <N></N>
)
